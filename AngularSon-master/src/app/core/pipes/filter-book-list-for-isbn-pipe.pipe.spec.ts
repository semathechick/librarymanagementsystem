import { FilterBookListForIsbnPipePipe } from './filter-book-list-for-isbn-pipe.pipe';

describe('FilterBookListForIsbnPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBookListForIsbnPipePipe();
    expect(pipe).toBeTruthy();
  });
});
